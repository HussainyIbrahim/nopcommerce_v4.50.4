@using Nop.Web.Areas.Admin.Models.Error

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model ErrorSearchModel
@{
    //page title
    ViewBag.PageTitle = T("Admin.Catalog.Errors").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Errors");
}
<div class="content-header clearfix">
    <h1 class="float-left">
        @T("Admin.Catalog.Errors")
    </h1>
    <div class="float-right">
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus-square"></i>
            @T("Admin.Common.AddNew")
        </a>
    </div>
</div>
<section class="content">
    <div class="container-fluid">
        <div class="form-horizontal">
            <div class="cards-group">
                <div class="card card-default">
                    <div class="card-body">
                        @await Html.PartialAsync("Table", new DataTablesModel
                        {
                            Name = "languages-grid",
                            RowIdBasedOnField = nameof(ErrorModel.Code),
                            UrlRead = new DataUrl("List", "Error", null),
                            Length = Model.PageSize,
                            LengthMenu = Model.AvailablePageSizes,
                            ColumnCollection = new List<ColumnProperty>
                            {
                                new ColumnProperty(nameof(ErrorModel.Code))
                                {
                                    Title = T("Admin.Catalog.Errors.Fields.Code").Text
                                },
                                new ColumnProperty(nameof(ErrorModel.Id))
                                {
                                    Title = T("Admin.Common.Edit").Text,
                                    Width = "100",
                                    ClassName =  NopColumnClassDefaults.Button,
                                    Render = new RenderButtonEdit(new DataUrl("Edit"))
                                }
                            }
                        })
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>